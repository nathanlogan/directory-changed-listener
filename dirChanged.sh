#!/bin/sh

# This file is where you do whatever you need to do when a change is noticed.
# It should be in the same directory as "dirListener.sh".
# e.g. hotfix a "working file" into your local deployment directory without doing a full deployment

# Here's the simplest of examples:

ls
